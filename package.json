{
  "name": "poseidon",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "color-js": "^1.0.4",
    "fs-extra": "^6.0.1",
    "glamor": "^2.20.40",
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build:react": "react-scripts build",
    "build:xml": "node bin/build-xml.js",
    "build": "npm run build:react && npm run build:xml",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "gh-pages --dist build",
    "build-and-deploy": "npm run build && npm run deploy"
  },
  "homepage": ".",
  "devDependencies": {
    "gh-pages": "^1.1.0",
    "glob": "^7.1.2",
    "remark-parse": "^5.0.0",
    "unified": "^7.0.0",
    "xml-trident": "^1.0.1-rc1"
  }
}
